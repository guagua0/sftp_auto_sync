# 远程盘功能开发任务

## 目标

当前程序已经支持"本地目录 -> 远程服务器目录"的自动同步。

现在要新增一条新的主线能力：

- 把远程服务器上的某个目录映射成一个 Windows 盘符
- 在资源管理器中能看到远程目录和文件列表
- 文件内容默认不提前下载
- 当有程序真正读取文件时，再自动下载到本地缓存
- 本次程序运行期间，已经下载过的文件优先直接走本地缓存，不重复从服务器下载
- 文件被修改后，自动回传上传到服务器
- 程序重启后，内存映射清空；下次运行时再按需重新下载

这份文档只关注"先把产品做成可用"的主线，不在当前阶段打磨次要细节。

## 当前状态

已完成：

- 远程盘映射配置的数据模型、数据库表、仓储、服务层
- UI 中远程盘配置的新增、编辑、删除、列表展示
- 远程目录元数据缓存
- 文件内容懒下载到本地缓存
- 运行期内存缓存索引
- 本地缓存文件复用
- 文件打开、读取、写入、刷新、关闭等会话逻辑
- 脏文件上传调度
- 重命名、删除等基础文件操作
- 虚拟文件系统门面层
- **WinFSPy 运行时探测（winfspy_runtime.py）**
- **WinFSPy 适配器实现（winfspy_adapter.py）**
- **MountManager 真实挂载和卸载逻辑（已切换到 WinFSPy）**
- **UI 状态显示更新（挂载成功/失败、盘符信息）**
- **Python 3.9 兼容性修复（移除 slots=True）**
- **后台线程挂载，避免 UI 阻塞**

当前状态：**第一阶段核心框架已完成，WinFSPy 基础挂载测试通过，等待完整功能验证**

## 技术选型变更

### 从 Dokany 切换到 WinFSPy

原计划使用 Dokany 实现虚拟盘挂载，但 PyPI 上的 `dokan` 模块 bug 太多，无法正常使用。

现已切换到 **WinFSPy**（WinFSP 的 Python 绑定）：

- WinFSPy 更稳定，API 设计更清晰
- 通过 `pip install winfspy` 安装
- 需要 Python 3.9 环境
- 已验证可以成功挂载虚拟盘符

相关文件：
- `sftp_auto_sync/remote_drive/winfspy_runtime.py` - 运行时检测
- `sftp_auto_sync/remote_drive/winfspy_adapter.py` - 文件系统适配器
- `sftp_auto_sync/remote_drive/dokany_adapter.py` - 保留但不再使用
- `sftp_auto_sync/remote_drive/dokany_runtime.py` - 保留但不再使用

## 开发主线

### 第一阶段：打通最小可用挂载

目标：先让远程目录真的能挂成 Windows 盘，并且可以在资源管理器里浏览、打开、修改、保存普通文件。

必须完成：

1. ~~在 `dokany_adapter.py` 中实现最小可用的 Dokan ctypes 桥接~~ → **已切换到 WinFSPy**
2. ✅ 在 `winfspy_adapter.py` 中实现 `BaseFileSystemOperations`
3. ✅ 把 WinFSPy 回调转发到现有 `VirtualDriveFacade`
4. ✅ 在 `mount_manager.py` 中实现真实挂载和卸载
5. ✅ 挂载成功后，UI 状态从"仅运行时可用"切到"真实盘符已挂载"
6. 🔄 完善错误处理和连接超时

WinFSPy 必须实现的回调：

- `get_volume_info` - 卷信息
- `get_security_by_name` - 安全描述符
- `create` - 创建/打开文件
- `open` - 打开文件
- `close` - 关闭文件
- `cleanup` - 清理
- `read` - 读取文件
- `write` - 写入文件
- `flush` - 刷新缓冲
- `get_file_info` - 获取文件信息
- `get_dir_info_by_name` - 列出目录内容
- `can_delete` - 检查是否可删除
- `delete` - 删除文件/目录
- `rename` - 重命名

这一阶段的验收标准：

- 可以从 UI 发起挂载
- Windows 资源管理器能看到盘符
- 能列出远程目录文件
- 双击文本文件可以打开
- 第一次读取时会自动下载到缓存
- 第二次读取同一文件时直接命中缓存
- 修改并保存后，能自动上传回服务器
- 卸载后盘符消失

### 第二阶段：补齐最基本稳定性

目标：确保最小可用方案不是"只能演示"，而是"可以连续使用"。

必须处理：

1. WinFSPy 回调中的异常不能让整个进程失控
2. 打开句柄和关闭句柄必须成对，避免缓存文件被错误占用
3. 写入后刷新和关闭时要正确触发上传
4. 遇到服务器断连时，给出明确错误并避免挂死
5. 卸载时要正确停止会话、停止上传线程、释放 WinFSPy 文件系统
6. UI 里要能明确显示挂载成功、挂载失败、运行中、已卸载

这一阶段不追求完整文件系统语义，只追求"不容易坏死"。

### 第三阶段：补必要缺口

只补真正影响可用性的缺口，不提前做大而全。

候选项：

- 新建文件
- 新建目录
- 删除文件
- 删除目录
- 重命名
- 截断文件
- 更准确的文件时间和大小回写
- Explorer 兼容性问题修正

原则：

- 只有在手工验证时发现"缺这个就没法正常使用"，才立刻补
- 没被实际场景卡住的点，先不做

## 具体待办

### A. WinFSPy 适配器完善

- ✅ 实现 `WinFSPyFileSystemOperations` 类
- ✅ 实现基本文件操作回调
- 🔄 完善错误处理和异常转换
- 🔄 处理文件上下文生命周期
- 🔄 处理并发访问安全

### B. 适配现有虚拟文件系统

- ✅ `create/open` 接到 `open_file`
- ✅ `read` 接到 `read_handle`
- ✅ `write` 接到 `write_handle`
- ✅ `flush` 接到 `flush_handle`
- ✅ `close/cleanup` 接到 `close_handle`
- ✅ `get_file_info` 接到 `stat_file`
- ✅ `get_dir_info_by_name` 接到 `list_dir`
- ✅ `rename` 接到 `rename_file`
- ✅ `delete` 接到 `delete_file`

### C. 挂载管理

- ✅ `mount_manager.py` 中保存 WinFSPy 文件系统实例
- ✅ 挂载成功后记录盘符已挂载
- ✅ 卸载时正确停止文件系统
- ✅ 卸载完成后再关闭远程会话和连接
- ✅ 避免重复挂载同一个 mapping
- ✅ 后台线程执行挂载，避免 UI 阻塞
- ✅ 挂载超时保护

### D. UI 状态修正

- ✅ 挂载成功时显示真实运行状态
- ✅ 挂载失败时展示明确错误
- ✅ 区分"会话已启动但盘符未挂载"和"盘符已挂载"
- ✅ 在远程盘列表中显示盘符、后端状态、待上传数量
- ✅ 更新能力显示对话框为 WinFSPy 信息

### E. 手工验证

必须做一轮从 UI 到 Explorer 的真机验证：

1. 新建一个远程盘映射
2. 配置 SFTP 服务器连接
3. 点击挂载
4. 在资源管理器中打开盘符
5. 浏览目录
6. 打开一个文本文件
7. 确认首次读取后生成本地缓存
8. 再次打开同一文件，确认直接走缓存
9. 修改保存文件
10. 确认文件被回传到服务器
11. 点击卸载
12. 确认盘符消失

## 当前优先级

按顺序执行，不要分散：

1. ✅ WinFSPy 基础挂载测试
2. ✅ 切换 mount_manager 到 WinFSPy
3. ✅ Python 3.9 兼容性修复
4. 🔄 完整挂载功能验证（需要 SFTP 服务器）
5. 补阻塞可用性的缺口
6. 最后再补测试和优化

## 当前不优先做的事情

以下内容暂时不作为主线：

- 跨平台支持
- 完整 POSIX 权限语义
- 复杂文件属性同步
- 预下载优化
- 缓存淘汰策略
- 大规模性能调优
- 过度抽象的重构
- 很细的 UI 打磨

原因很简单：

当前最大问题不是"功能不够精致"，而是"还没有真的挂出来一个可用的远程盘"。

## 完成标准

当满足下面几点时，认为这一轮主功能完成：

- 用户可以在 UI 中配置远程盘
- 用户可以点击挂载并得到真实 Windows 盘符
- 用户可以像普通磁盘一样浏览远程目录
- 文件按需下载到本地缓存
- 本次运行期间重复读取同一文件时直接命中缓存
- 修改后的文件可以自动上传回服务器
- 用户可以正常卸载

达到以上标准后，再进入后续优化阶段。

## 开发环境

```bash
# 激活开发环境
conda activate py39

# 安装依赖
pip install -r requirements.txt
pip install winfspy

# 运行程序
python -m sftp_auto_sync.app.main

# 运行测试
pytest sftp_auto_sync/tests/
```
