# SFTP Auto Sync 使用教程

一款 Windows 桌面应用程序，用于监控本地目录并将文件变更自动上传到远程 SFTP 服务器。

---

## 目录

- [安装与运行](#安装与运行)
- [界面概览](#界面概览)
- [配置服务器](#配置服务器)
- [配置映射](#配置映射)
- [忽略模式说明](#忽略模式说明)
- [启动同步](#启动同步)
- [查看日志](#查看日志)
- [设置](#设置)
- [常见问题](#常见问题)

---

## 安装与运行

### 方式一：源码运行

```bash
# 安装依赖
pip install -r requirements.txt

# 启动程序
python -m sftp_auto_sync
```

### 方式二：双击 run.bat

Windows 用户可以直接双击 `run.bat` 启动程序。

### 数据存储位置

程序数据存储在 `%APPDATA%/SFTPAutoSync/` 目录下：

```
%APPDATA%/SFTPAutoSync/
├─ app.db          # 配置数据库
├─ known_hosts     # SSH 主机密钥
├─ logs/           # 日志目录
│  ├─ app.log
│  └─ error.log
└─ cache/
```

---

## 界面概览

程序包含以下页面：

| 页面 | 功能 |
|------|------|
| Dashboard | 查看总体状态、服务器连接情况、最近同步记录 |
| Servers | 管理 SFTP 服务器配置 |
| Mappings | 管理本地-远程目录映射 |
| Logs | 查看同步历史记录 |
| Settings | 应用设置 |

---

## 配置服务器

### 添加服务器

1. 点击左侧导航栏的 **Servers**
2. 点击 **添加** 按钮
3. 填写服务器信息：

| 字段 | 说明 |
|------|------|
| 名称 | 服务器标识名称（唯一） |
| 主机 | SFTP 服务器地址 |
| 端口 | 默认 22 |
| 用户名 | SSH 登录用户名 |
| 认证方式 | 密码 / 私钥 |
| 密码 | 认证方式为密码时填写 |
| 私钥路径 | 认证方式为私钥时选择文件 |
| 私钥口令 | 私钥加密时的口令（可选） |
| 连接超时 | 默认 10 秒 |
| 主机密钥策略 | TOFU（首次信任）/ Strict（严格验证） |

4. 点击 **保存**

### 测试连接

选中服务器后，点击 **测试连接** 按钮验证配置是否正确。

### 启用/禁用

选中服务器后，点击 **启用/禁用** 按钮切换状态。

---

## 配置映射

### 添加映射

1. 点击左侧导航栏的 **Mappings**
2. 确保已创建至少一个服务器
3. 点击 **添加** 按钮
4. 填写映射信息：

| 字段 | 说明 |
|------|------|
| 名称 | 映射标识名称（唯一） |
| 服务器 | 选择目标 SFTP 服务器 |
| 本地目录 | 要监控的本地文件夹（点击浏览选择） |
| 远程目录 | SFTP 上的目标路径（如 `/var/www/project`） |
| 递归 | 是否监控子目录（默认开启） |
| 删除策略 | 忽略（默认）/ 删除文件 |
| 启动时扫描 | 应用启动时检查文件变更（默认开启） |
| 忽略模式 | 不需要同步的文件模式（详见下文） |
| 备注 | 可选说明 |
| 启用 | 是否启用此映射 |

5. 点击 **保存**

### 删除策略说明

- **忽略**（默认）：本地删除文件后，远程文件保留不变
- **删除文件**：本地删除文件后，远程对应文件也会被删除

---

## 忽略模式说明

忽略模式使用 **glob 模式**，每行写一个规则。

### 基本语法

| 模式 | 说明 | 示例 |
|------|------|------|
| `*` | 匹配任意字符（不含 `/`） | `*.tmp` 匹配所有 .tmp 文件 |
| `**` | 匹配任意层级目录 | `**/*.log` 匹配所有 .log 文件 |
| `?` | 匹配单个字符 | `file?.txt` 匹配 file1.txt |
| `[abc]` | 匹配括号内任一字符 | `file[12].txt` |
| `{a,b}` | 匹配多个选项 | `*.{jpg,png}` |

### 常用忽略模式

#### 推荐默认配置

```
.git/**
.idea/**
.vscode/**
**/.DS_Store
**/Thumbs.db
**/*.swp
**/*.tmp
**/*~
```

#### 前端项目

```
node_modules/**
dist/**
build/**
.cache/**
*.map
```

#### Python 项目

```
__pycache__/**
*.pyc
*.pyo
.venv/**
venv/**
*.egg-info/**
dist/**
build/**
```

#### 编辑器临时文件

```
**/*.swp
**/*.swo
**/*~
**/.~*
```

#### 系统文件

```
**/.DS_Store
**/Thumbs.db
**/desktop.ini
```

### 使用方式

在 **映射编辑** 对话框中的 **忽略模式** 文本框中，每行输入一个模式：

```
.git/**
node_modules/**
dist/**
*.tmp
*.log
```

---

## 启动同步

### 方式一：自动启动

如果在设置中开启了 **启动时自动开始引擎**，程序启动后会自动开始同步所有已启用的映射。

### 方式二：手动启动

1. 配置好服务器和映射
2. 点击 **Dashboard** 页面
3. 查看同步状态

### 同步机制

1. **实时监听**：使用 watchdog 监听文件变化，自动上传新增和修改的文件
2. **启动补扫**：应用关闭期间的文件变更，会在下次启动时自动同步
3. **事件聚合**：短时间内多次保存同一文件不会重复上传（800ms 去抖）
4. **失败重试**：上传失败会自动重试，最多 5 次

### 上传协议

- 文件先上传为临时文件 `filename.__uploading__`
- 上传完成后原子重命名为正式文件
- 确保远程不会出现不完整的文件

---

## 查看日志

### 日志页面

点击左侧导航栏的 **Logs** 查看同步历史记录。

支持筛选：
- 映射
- 服务器
- 状态（成功/失败）
- 关键字

### 日志文件

日志文件位于 `%APPDATA%/SFTPAutoSync/logs/`：

- `app.log`：INFO 及以上级别
- `error.log`：ERROR 及以上级别

日志自动滚动，每个文件最大 5MB，保留 5 个备份。

---

## 设置

点击左侧导航栏的 **Settings** 进入设置页面。

| 设置项 | 说明 |
|--------|------|
| 应用数据目录 | 只读，显示配置文件位置 |
| 日志保留行数 | 历史记录最大保留条数（默认 20000） |
| 去抖毫秒数 | 文件变更防抖间隔（默认 800ms） |
| 启动时自动开始引擎 | 程序启动后自动开始同步 |

---

## 常见问题

### Q: 忽略模式保存后丢失

**原因**：旧版数据库可能缺少 `ignore_patterns_json` 列。

**解决**：已修复。更新代码后重新启动程序即可。

### Q: 文件没有自动同步

**检查项**：
1. 映射是否已启用
2. 服务器是否已启用
3. 文件是否匹配了忽略模式
4. 查看 Logs 页面是否有错误信息

### Q: 连接服务器失败

**检查项**：
1. 使用 **测试连接** 按钮验证配置
2. 确认主机地址、端口、用户名正确
3. 确认密码或私钥路径正确
4. 检查防火墙是否允许 SSH 连接

### Q: 远程目录不存在

程序会在上传文件时自动创建远程目录。确保远程目录路径以 `/` 开头（POSIX 格式）。

### Q: 如何停止同步

程序关闭后自动停止所有同步任务。下次启动会通过补扫恢复期间的文件变更。

### Q: 多个映射指向同一本地目录

不允许。程序会检测并阻止重叠的映射配置，避免同一文件被多次处理。

### Q: 中文路径支持

支持。本地路径和远程路径均可包含中文字符。

---

## 快速上手步骤

1. 启动程序
2. 点击 **Servers** → **添加** → 填写服务器信息 → **保存**
3. 点击 **测试连接** 确认配置正确
4. 点击 **Mappings** → **添加** → 选择服务器和本地目录 → **保存**
5. 程序自动开始同步
6. 在 **Dashboard** 查看同步状态
7. 在 **Logs** 查看历史记录

---

## 技术说明

- **GUI 框架**：PySide6 (Qt for Python)
- **文件监听**：watchdog
- **SFTP 连接**：paramiko
- **配置存储**：SQLite
- **密码存储**：系统密钥库 (keyring)
